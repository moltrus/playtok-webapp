Launch windows command prompt everytime as terminal in VS Code whenever you open a new terminal. Use command prompt syntax in terminal.

```json
{
  "terminal.integrated.defaultProfile.windows": "Command Prompt"
}
```